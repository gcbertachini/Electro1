Importante para correr con el mos BS170:

Copiar el archivo "standard.mos" a la carpeta "LTSPICE<VER>\lib\cmp" y reemplazar el archivo (básicamente ya agregué el BS170 a la librería interna de LTSPICE)